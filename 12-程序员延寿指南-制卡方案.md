# 《程序员延寿指南》(HowToLiveLonger) → Anki 制卡

> 2026-09-26　|　**已执行：34 张入库**
> 源：https://github.com/geekan/HowToLiveLonger

## 一、项目研判

| 项 | 实测 |
|---|---|
| 规模 | 单个 README.md（306 行），主体是「6. 证据」 |
| 结构 | 6.1 输入（固体/液体/气体/光照/药物 5 子节）· 6.2 输出（8 子节）· 6.3 上下文（4 子节） |
| 条目 | 一级主题 42 个 / 二级研究 73 个 |
| **格式** | **嵌套列表**（3 层）+ 双来源（中文文章 + 英文原文）+ 外链图片 |
| 图片 | 17 处引用，**3 个是 shields 徽章**（排除）→ 实际 14 张 |
| 争议标注 | 2 个主题带「（存有大量争议）」 |

## 二、切分方案（用户拍板）

| 节 | 切法 | 张数 |
|---|---|---|
| 6.1 输入 | 按**一级主题** | 25 → 实得 **21**（4 个主题原文只有链接/空，跳过） |
| 6.2 输出 | 按**子节**（一级其实是文章标题） | 8 |
| 6.3 上下文 | 按**子节** | 4 |
| 第 5 节 行动 | **单独总览卡** | 1 |
| | | **34** |

## 三、牌组结构

```
20：在用::20.11：《程序员延寿指南》
├── 20.11.01：输入      21
├── 20.11.02：输出       8
├── 20.11.03：上下文     4
└── 20.11.04：行动总览   1
```

## 四、字段映射

| 字段 | 内容 |
|---|---|
| 正文 | 主题名（粗体小标题）+ 各研究结论 + 图片 |
| 出处 | 主题名 / 子节名 |
| 来源 | 中文文章标题 + 链接（`<a href>`） |
| 备注 | 英文原文标题（`出处：...`） |
| 类型 | 方法（通读 → 单面卡） |
| 标签 | `HowToLiveLonger` `分类-X` + `争议`（如主题名带争议） |

## 五、执行结果

| 项 | 结果 |
|---|---|
| 卡片总数 | **34** |
| 单面（自测空） | 34 |
| 正反完全相同（渲染验证） | 34 |
| 无未转义 `<` | 34 |
| **内容完整**（与原文逐字比对） | **34** |
| 图片 | 12 张（媒体库零缺失，**已注册 media.db**） |

## 六、过程中修的 bug

| # | bug | 说明 |
|---|---|---|
| 1 | **tab 缩进被算成一级** | 仓库混用 tab，`\t* 出处2：` 被误判成新主题 → "辣椒"正文空。改为 tab 按 4 空格算 |
| 2 | **正文混进文章标题** | 二级「文章标题」应只进来源，不进正文 |
| 3 | **来源里 markdown 链接没转 `<a>`** | `inline()` 只处理了粗体，漏了链接 |
| 4 | **跨句粗体被拆坏** | `split_paras` 把 `**...**` 拆到两段 → 标签不成对。改为含 `**` 的段落不切分 |
| 5 | **主题重名互相覆盖** | 「亚精胺」「综合」在不同子节重复 → 写入时覆盖。改为冲突时加子节名 |
| 6 | **纯图片行被跳过** | 图片提取后 `txt` 为空，被 `if not txt: continue` 提前跳过 |
| 7 | 空正文卡不显示 | 原文只有标题/链接的主题（如 NMN）→ 跳过 |
| 8 | **媒体没注册 media.db** | 图片只放目录不注册 → 客户端看不到（上次踩过，这次注意了） |
| 9 | 图片 0 字节文件 | 下载失败仍创建文件 → 清理并修正引用 |

## 七、验证脚本自身的 bug（又骗了我 3 轮）

1. 只查「正文+来源」，**漏了备注**
2. 没去原文的 `**` 标记（卡片是 `<strong>`，两边对不上）
3. 没处理"文章标题"的位置差异

**教训（第三次强化）**：验证脚本的解析必须与生成脚本对齐。这次我又是先怀疑卡片、追查后发现是脚本。

## 八、基础设施坑

- 长任务（34 张 × 8 秒）会因 SSH 断连而中断 → **用 `nohup ... &` 脱离 SSH**
- Anki API 导入的图片在工作库，**还要复制到同步服务器目录 + 注册 media.db**
