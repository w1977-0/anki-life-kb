# 《程序员做饭指南》(HowToCook) → Anki 制卡

> 2026-09-26　|　**已执行：390 张入库**（372 道菜 + 18 篇 tips）
> 源：https://github.com/Anduin2017/HowToCook （Unlicense）

## 一、项目研判

| 项 | 实测 |
|---|---|
| 规模 | 372 道菜谱 + 18 篇 tips |
| 分类 | 11 个（荤菜111 素菜64 主食59 水产28 早餐25 汤24 饮品23 甜点19 半成品10 调料9 + 模板1） |
| 菜谱结构 | 简介 / 难度★ / 卡路里 / 必备原料 / 计算（用量）/ 操作（步骤）/ 图片 / 附加内容 |
| 字段一致性 | **100%**（373/373 全含难度/卡路里/原料/计算/操作） |
| 图片 | 353 张 / 108MB |
| **目录有两种** | 两层（`分类/菜.md`，172 个，无图）+ 三层（`分类/菜名/菜名.md`，201 个，图在同目录） |
| **图片重名** | 严重 —— `1.jpeg` 被 22 道菜复用 |
| 难度分布 | ★34 / ★★98 / ★★★131 / ★★★★89 / ★★★★★21 |

## 二、牌组结构（11 分类直接做）

```
20：在用::20.10：《程序员做饭指南》
├── 20.10.01 荤菜 111    ├── 20.10.06 汤 24
├── 20.10.02 素菜 64     ├── 20.10.07 饮品 23
├── 20.10.03 主食 59     ├── 20.10.08 甜点 19
├── 20.10.04 水产 28     ├── 20.10.09 半成品 10
├── 20.10.05 早餐 25     ├── 20.10.10 调料 9
                        └── 20.10.11 烹饪技法 18（tips）
```

## 三、字段映射（沿用 9 字段，不加新字段）

| 字段 | 内容 |
|---|---|
| 正文 | 简介 + 难度/卡路里 + 原料(`<ul>`) + 计算(`<ul>`) + 操作(`<ol>`) + 图片 |
| 出处 | 菜名 |
| 我的话 | 空 |
| 类型 | 方法（通读 → 单面卡） |
| 自测 | 空（单面，点击无变化） |
| 来源 | `HowToCook 程序员做饭指南 · <分类>/<菜名>` |
| 备注 | 附加内容的非模板部分（370/372 是模板文本，已过滤） |

**标签**：`HowToCook` `分类-X` `难度-★★★` `卡路里-223`

## 四、图片处理

- 重命名 `htc-<菜名>-<原名>.<ext>` 防冲突（仓库里 `1.jpeg` 被 22 道菜复用）
- 导入 `collection.media`
- 正文里用 `<img src="...">` 引用（原位置的 markdown 语法一并替换）

## 五、执行结果

| 项 | 结果 |
|---|---|
| 卡片总数 | **390**（372 菜 + 18 tips） |
| 单面（自测空） | 390 |
| 正反完全相同（渲染验证） | 390 |
| 无未转义 `<` | 390 |
| 内容完整（与原文逐字比对） | **390** |
| 含图卡片 | 164 |
| 副本同步 | 9671/9671 一致 |

## 六、过程中修的 bug

| # | bug | 说明 |
|---|---|---|
| 1 | 卡路里丢"大卡"单位 | 正文保留单位，标签用纯数字 |
| 2 | 图片重复 | 原位置 + 末尾各一次 → 只保留原位置 |
| 3 | 图片被吸进 `<ol>` | 图片行单独成块 |
| 4 | 正文残留 `![alt](path)` | 转 `<img>` |
| 5 | tips 编号步骤没转 `<ol>`、`**粗体**` 没转 | 补编号列表 + 行内粗体 |
| 6 | **tips glob 重复**（21 篇 ≠ 18 篇） | `**/*.md` 与 `*.md` 重复计根目录 3 篇 → 删 3 张重复卡 |
| 7 | 2 张早餐卡重复写入（查重漏网） | 删掉后写入的那 2 张 |
| 8 | 1 张素菜卡被查重拦下 | 手动补写 |
| 9 | **磁盘坑**：`/tmp` 是 tmpfs 仅 483M | 102MB 图片包放不下 → 改到根分区解压 |
| 10 | `sudo cp dir/*` glob 权限失败 | 改 `sudo sh -c "cp ..."` |

## 七、验证脚本自身的 bug（导致误报，共 4 类）

1. 没剥 `###` 子标题前缀
2. 没剥 `>` 引用块前缀
3. 图片行**带编号**时（`11. ![x](y)`）没跳过
4. "文字+图片"同行（`- 鲜面条（扯面尤佳）![图](x)`）没处理

**教训**：验证脚本的解析逻辑必须与生成脚本对齐，否则会冤枉自己（误报"内容缺失"）。
