# 《提问的智慧》(How-To-Ask-Questions-The-Smart-Way) → Anki 制卡

> 2026-09-26　|　**已执行：36 张入库**
> 源：https://github.com/ryanhanwu/How-To-Ask-Questions-The-Smart-Way
> 原文：Eric S. Raymond, Rick Moen（中文翻译 Ryan Wu / Gasolin）

## 一、项目研判

| 项 | 实测 |
|---|---|
| 规模 | 单文件 `README-zh_CN.md`（675 行），繁简两版结构一致 |
| 结构 | **12 个 H2**（排除导航），其中 **2 个纯容器**（「当你提问时」0 字、「如何解读答案」17 字） |
| 小节 | **26 个 H3**（当你提问时 23 + 如何解读答案 3） |
| 内容 | 35 个内容单元，中位 524 字 / 最长 1721 字（「不该问的问题」） |
| 图片 | 仅 2 个 shields 徽章（已排除）→ **无媒体工作量** |

## 二、切分方案（用户拍板）

| 类型 | 张数 |
|---|---|
| H3 一张卡 | 26 |
| 有内容 H2 各一张（含「相关资源」「鸣谢」） | 10 |
| **合计** | **36** |

**纯容器 H2 不出卡**，只作为牌组承载其 H3。

## 三、牌组结构（12 个）

```
20：在用::20.12：《提问的智慧》
├── 20.12.01 声明 1      ├── 20.12.07 不该问的问题 1
├── 20.12.02 简介 1      ├── 20.12.08 好问题与蠢问题 1
├── 20.12.03 在提问之前 1  ├── 20.12.09 如果得不到回答 1
├── 20.12.04 当你提问时 23 ├── 20.12.10 如何更好地回答问题 1
├── 20.12.05 如何解读答案 3 ├── 20.12.11 相关资源 1
├── 20.12.06 如何避免扮演失败者 1 └── 20.12.12 鸣谢 1
```

## 四、字段映射

| 字段 | 内容 |
|---|---|
| 正文 | H3/H2 原文（`<p>` 段落 + `<ul>` 列表 + `<strong>` 粗体 + `<a>` 链接 + `<code>` 代码） |
| 出处 | H3 标题 / H2 标题 |
| 类型 | 方法（通读 → 单面卡） |
| 来源 | 《提问的智慧》· 章节 [/ 小节] |
| 标签 | `提问的智慧` `章节-X` |

**分段参数**：**220 字 / 8 句**（实测选定；对比过 140/5、180/6、260/10）

## 五、执行结果

| 项 | 结果 |
|---|---|
| 卡片总数 | **36** |
| 单面（自测空） | 36 |
| 正反完全相同（渲染验证） | 36 |
| 无未转义 `<` + 无残留 md | 36 |
| **内容完整** | **36 / 36** |
| 残留 `**` / `[x](y)` | 0 |

## 六、复习节奏（已设）

- 「当你提问时」：**5 张/天**
- 其余 11 个牌组：**0**（冻结）

## 七、这次的关键教训：验证脚本第 4、5 次骗我

**现象**：内容完整性一度只有 5/36，我逐张排查后确认**卡片全是对的**，问题都在验证脚本：

| # | 验证脚本的 bug |
|---|---|
| 4 | 原文 `**` **跨段落**（分处两段），按句切分后不成对 → 与卡片的 `<strong>` 对不上 |
| 5 | 原文 `*` **列表项之间没有句末标点**，按句切分会把多项**并成一句**，带着 `*` 去比对 |

**最终解法**：**放弃按句切分**，改用
```
原文 → 去所有 md 标记 → 去空白  ┐
卡片 → 去所有 HTML 标签 → 去空白 ├→ difflib 相似度 ≥ 0.98 判为完整
```
**不依赖切分规则**，一次通过 36/36。

**教训（第 5 次强化）**：
> 验证脚本一旦开始"打补丁"，说明**方法本身错了** —— 应该换方法，而不是继续补。
